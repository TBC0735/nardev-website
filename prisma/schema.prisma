// Schéma de base de données Nardev — brouillon commun à valider en équipe.
// cf. cahier des charges v2, section 4 : tables Service, Projet, Message, Membre, User (admin).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

/// Compte administrateur (back-office /admin) — auth NextAuth Credentials.
model User {
  id           String   @id @default(cuid())
  email        String   @unique
  name         String?
  passwordHash String
  createdAt    DateTime @default(now())
}

/// Les 4 services présentés sur / et /services (contenu géré depuis l'admin).
/// Domaine : Mame Diarra
model Service {
  id          String   @id @default(cuid())
  slug        String   @unique
  titre       String
  /// Phrase courte pour la carte service sur l'accueil.
  resume      String
  /// Texte long (3-4 phrases) pour la page /services.
  description String
  /// Pourquoi choisir ce service — bénéfices pour le client.
  avantages   String[]
  /// Liste à puces « ce que le client reçoit » — un élément par ligne.
  pointsCles  String[]
  /// Photo/illustration du service. Nullable : un dégradé de remplacement
  /// s'affiche tant que la vraie photo n'a pas été ajoutée depuis l'admin.
  imageUrl    String?
  ordre       Int      @default(0)
  projets     Projet[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

/// Réalisations affichées sur /portfolio et /portfolio/[slug].
/// Domaine : Rokhaya
model Projet {
  id           String   @id @default(cuid())
  slug         String   @unique
  nom          String
  imageUrl     String?
  /// Besoin client + solution apportée.
  description  String
  technologies String[]
  service      Service? @relation(fields: [serviceId], references: [id])
  serviceId    String?
  publie       Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

/// Messages reçus via le formulaire /contact.
/// Domaine : Awa Ndao
model Message {
  id        String   @id @default(cuid())
  nom       String
  entreprise String?
  service   String?
  contenu   String
  lu        Boolean  @default(false)
  createdAt DateTime @default(now())
}

/// Fondateurs présentés sur /a-propos.
/// Domaine : Ndiawar
model Membre {
  id           String   @id @default(cuid())
  nom          String
  role         String
  presentation String
  photoUrl     String?
  ordre        Int      @default(0)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
